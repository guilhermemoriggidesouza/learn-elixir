IO.puts "Hello world te amo amor"

#atoms (enums)
#true and false are atoms
#when the First letter is uppercase also is atom
IO.puts :apple
IO.puts :apple == :apple
IO.puts :apple == :orange
IO.puts is_atom(true)
IO.puts is_atom(Hello)


